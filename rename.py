def rename(m_type, search):
    if m_type==1:   #한신평
        if search=='가나다':
            return '으엑'
        else:
            return search
    elif m_type==2: #나신평
        return search
    elif m_type==3: #한기평
        return search
